@model List<EmlakSistemi.Models.EngelliUygunluk>

@{
    Layout = null;
}

@{ 
    var goruntuleme = "";
    for (int i = 0; i < Model.Count; i++)
    {
        goruntuleme += Model[i].EngelliUygunluk_AD + " | ";
    }
}
<span>@goruntuleme</span>


